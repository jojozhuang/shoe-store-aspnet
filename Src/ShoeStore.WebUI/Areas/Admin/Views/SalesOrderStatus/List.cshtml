@model IEnumerable<Johnny.ShoeStore.Domain.Entities.SalesOrderStatus>

@{
    ViewBag.Title = "Admin: All Sales Order Status";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="box-body">
    <table id="example1" class="table table-bordered table-striped table-hover">
        <thead>
            <tr>
                <th class="text-right">ID</th>
                <th>Sales Order Status Name</th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="text-right">@item.SalesOrderStatusId</td>
                    <td>@Html.ActionLink(item.SalesOrderStatusName, "Edit", new { item.SalesOrderStatusId })</td>
                    <td class="text-center">
                        @using (Html.BeginForm("Delete", "SalesOrderStatus"))
                        {
                            <a href="@Url.Action("Edit", "SalesOrderStatus", new { item.SalesOrderStatusId })" class="btn btn-default btn-xs">
                                <span class="glyphicon glyphicon-edit" /> Edit
                            </a>
                            <button type="button" class="btn btn-default btn-xs" data-href="@Url.Action("Delete", "SalesOrderStatus", new { item.SalesOrderStatusId })" data-toggle="modal" data-target="#confirm-delete">
                                <span class="glyphicon glyphicon-remove"></span> Delete
                            </button>
                        }
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <th class="text-right">ID</th>
                <th>Sales Order Status Name</th>
                <th class="text-center">Actions</th>
            </tr>
        </tfoot>
    </table>
</div><!-- /.box-body -->
